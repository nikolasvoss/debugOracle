# Troubleshooting

If setup is partial, ask your agent to show DebugOracle's discovered inputs and next actions. Common causes are multiple ELF or SVD files, missing OpenOCD configuration, and a debugger session without a useful GDB/MI stop record.
