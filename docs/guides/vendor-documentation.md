# Manufacturer documentation

Put manuals and datasheets anywhere in `debugoracle-input/`, or leave them where they already live. PDF parsing requires explicit authorization through the agent and may take seconds to several minutes. Original PDFs are never modified or downloaded. Agent-driven workspace setup stores generated search data under `.dbgoracle/documentation-search/`; legacy sidecars remain readable.
