{
  "name": "signal_received_stop",
  "description": "Real STM32 debug session where execution stops with SIGINT in HAL_GetTick.",
  "questions": [
    {
      "id": "q1",
      "prompt": "What was the stop reason in this capture?",
      "expected_conclusion": "signal-received",
      "required_evidence": ["gdb_stop_event", "stack_frame"],
      "forbidden_behaviors": ["fabricating a stop reason"]
    }
  ]
}
